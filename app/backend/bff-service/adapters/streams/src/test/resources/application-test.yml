spring:
  application:
    name: bff-service
  main:
    allow-bean-definition-overriding: true
  redis:
    client-type: LETTUCE
    lettuce:
      pool:
        enabled: true


redis:
  streams:
    key: leads