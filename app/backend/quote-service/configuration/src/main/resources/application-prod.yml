spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: quotes
      username: root
      password: mongodb

eureka:
  client:
    enabled: true
    register-with-eureka: true

webservice:
  products:
    url: http://localhost:8080/products/v1