spring:
  application:
    name: lead-service
  main:
    allow-bean-definition-overriding: true
  #redis:
  #  username:
  #  password:
  #  port: 6379
  #  host: localhost
  #  ssl: false
  #  jedis:
  #    pool:
  #      enabled: true


redis:
  streams:
    key: leads
    group: ${spring.application.name}

eureka:
  client:
    enabled: false
    register-with-eureka: false